{% extends 'layouts/base.html.twig' %}
{% block body %}
  <div class="max-w-md mx-auto px-6 py-16">
    <h1 class="mb-6">Login</h1>
    <form method="post" class="space-y-4">
      <div>
        <label for="email" class="block text-sm font-medium text-light">Email</label>
        <input type="email" id="email" name="email" class="input-field" required>
      </div>
      <div>
        <label for="password" class="block text-sm font-medium text-light">Password</label>
        <input type="password" id="password" name="password" class="input-field" required>
      </div>
      <button type="submit" class="cta-button primary w-full">Login</button>
    </form>
    <p class="mt-4 text-center text-sm text-gray-400">
      Don’t have an account? <a href="{{ path('signup') }}" class="text-blue hover:text-pink">Sign Up</a>
    </p>
  </div>
{% endblock %}
