<div class="relative bg-gradient-to-r from-purple-500 to-indigo-600 py-24 text-center overflow-hidden">
    <div class="absolute inset-0 bg-black opacity-40"></div>
    <div class="relative z-10 container mx-auto px-6">
        <h1 class="text-5xl md:text-6xl font-bold text-white mb-6 animate-fade-in">
            <span class="text-transparent bg-clip-text bg-gradient-to-r from-neon-purple to-indigo-400">Claim Your Domain</span>
        </h1>
        <p class="text-xl md:text-2xl text-gray-200 mb-10">Launch your Web3 project with a custom domain like <span class="text-neon-purple font-semibold">yourname.aviyon.com</span> — powered by the Internet Computer.</p>
        <div class="max-w-lg mx-auto bg-gray-800 rounded-xl shadow-2xl p-8 border border-gray-700">
            <form action="{{ path('register_domain') }}" method="POST" class="flex flex-col gap-6" id="domain-form">
                <div class="flex items-center gap-4 bg-gray-700 rounded-lg p-2">
                    <input type="text" name="subdomain" placeholder="yourname" class="form-control flex-1 bg-transparent text-white placeholder-gray-400 focus:ring-0" required>
                    <select name="domain_extension" class="bg-gray-600 text-white rounded-lg p-2 focus:ring-2 focus:ring-neon-purple">
                        <option value=".aviyon.com">.aviyon.com</option>
                        <option value=".aviyon.co">.aviyon.co</option>
                        <option value=".aviyon.net">.aviyon.net</option>
                        <option value=".aviyon">.aviyon</option>
                    </select>
                </div>
                <div id="domain-availability" class="text-sm text-gray-400 mb-4 hidden"></div>
                <button type="submit" class="bg-neon-purple text-white px-8 py-4 rounded-lg text-lg font-semibold hover:bg-purple-700 transition transform hover:scale-105">Continue to Hosting</button>
            </form>
        </div>
    </div>
</div>
